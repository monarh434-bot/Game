# Voxel AI World MVP (Flat Version)

Плоская версия проекта: все файлы лежат в корне, без папок.

## Запуск

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```

Открыть:
`http://127.0.0.1:8000/`
